# important-requests

이 문서는 대화 중 사용자 발화에서 "중요 요청사항"만 추려 고정 저장한다.  
원문 로그는 `dialog.md`를 기준으로 확인한다.

## 기록 기준
- 아키텍처 원칙/권한/승인/운영 제약에 영향을 주는 요청
- 문서 구조/정합성 규칙을 바꾸는 요청
- 다른 에이전트가 반드시 알아야 할 작업 방식 변경

## 중요 요청 목록

1. `2026-04-11 17:58 (KST)`  
요청: 라우팅 분류는 `work/research` 라벨이 아니라 `destination_company` 기준으로 수행.

2. `2026-04-11 21:23 (KST)`  
요청: `dialog.md`에 사용자 원문 발화를 함께 남겨 다른 에이전트가 맥락을 볼 수 있게 유지.

3. `2026-04-11 21:23 (KST)`  
요청: 현재 범위는 아키텍처 설계이며 push/restart는 하지 않고 로컬 문서 계획으로만 진행.

4. `2026-04-11 22:44~22:57 (KST)`  
요청: `grant` 없는 실행 금지, `in_view`/`pending_approval` 구분, 용어집합 SSOT 문서화, 충돌 정의 명시.

5. `2026-04-11 23:34 (KST)`  
요청: 대화 중 중요 요청사항은 누락 없이 저장.

6. `2026-04-11 23:35 (KST)`  
요청: 기본 구조를 문서에 명시하고, `plan` 내용을 전체 아키텍처/운영 원칙 중심으로 정리해 모듈형 구조로 마이그레이션. 작업 전/후 커밋 수행.

7. `2026-04-12 11:31 (KST)`  
요청: 최우선 원칙 4가지(유연성/견고성/가시성/접근성)를 문서에 명시하고, `architecture` 폴더 정리 상태를 평가해 다시 정리.

8. `2026-04-12 12:46 (KST)`  
요청: 실시간성 관점에서 지연/미응답을 장애로 판단할 수 있어야 하며, 몇 분 이상 미응답 시 장애 판정 기준을 문서에 포함.

9. `2026-04-12 12:47 (KST)`  
요청: 앞으로 모든 수정 직전 사용자 확인을 받고, 사용자 답변이 `네`일 때만 수정.

10. `2026-04-12 13:48 (KST)`  
요청: 시스템 명령어에 `replan` 포함. 긴급 단축 명령은 `kill-switch=!!!!!!!`, `respawn=???????` 형태로 사용.

11. `2026-04-12 23:58 (KST)`  
요청: 시스템 커맨드는 우선 `kill-switch`를 확실히 고정하고, 나머지는 실사용하며 점진 튜닝.

12. `2026-04-13 07:50~07:51 (KST)`  
요청: DB 저장 스키마는 공통으로 유지하고, 강화 필드(`signature`, `nonce`, `sequence`)는 고위험 경로에서만 필수로 채우는 방식으로 확정.

13. `2026-04-13 08:00 (KST)`  
요청: 2번 항목은 하네스 무상태성과 전체 시스템 견고성 관점의 `B+` 모델로 확정해 문서에 반영.

14. `2026-04-13 08:03~08:04 (KST)`  
요청: 3번 항목은 단일 운영자 전제(`저밖에 안 씀`)의 `B-Solo` 모델로 확정하고 문서에 반영.

15. `2026-04-13 08:24~08:28 (KST)`  
요청: 4번 항목은 `B` 옵션(명령 유형별 timeout/retry 분리, transient만 재시도, `1분 warning/3분 incident`)으로 확정해 문서에 반영.

16. `2026-04-13 20:45 (KST)`  
요청: 이벤트 전달 신뢰성은 혼합 적용으로 정리. 전체는 `B`로 시작하고, 사용자 대상 필수 이벤트(알림/이메일/결제/승인 결과)는 `C(outbox+relay)`를 우선 적용.

17. `2026-04-13 21:28 (KST)`  
요청: 미논의 항목 중 `2=B`, `3=B`, `4=C`, `5=B`는 확정해 문서 반영. `1(rs 실행 조건)`과 `6(sm/rs/replan 튜닝 시나리오)`는 추가 구체 논의.

18. `2026-04-13 23:56 (KST)`  
요청: 자동 스위치 트리거는 `B`로 확정하고, `sm/rs/replan` 튜닝은 고정 순서 대신 다른 관점(상황 기반 프레임)으로 재정의해 논의.

19. `2026-04-14 00:49~00:54 (KST)`  
요청: 튜닝 시나리오는 고정 순서가 아니라 상황에 맞춰 유연하게 운용하고, 자동화는 기존 확정 범위(`sm` 자동 + `rs comm/harness` 제한 자동)와 충돌 없이 유지. 첨부 영어 문서는 한국어 번역본을 별도 파일로 저장.
